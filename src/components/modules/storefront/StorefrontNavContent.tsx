"use client";

import type { ReactNode } from "react";

/** Wrapper for storefront route content — nav progress UI removed for smoother transitions. */
export function StorefrontNavContent({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
